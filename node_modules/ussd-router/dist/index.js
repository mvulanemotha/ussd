"use strict";
var __spreadArray = (this && this.__spreadArray) || function (to, from) {
    for (var i = 0, il = from.length, j = to.length; i < il; i++, j++)
        to[j] = from[i];
    return to;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ussdRouter = exports.goToHome = exports.goBack = void 0;
var goBack = function (str, keyword) {
    if (keyword === void 0) { keyword = '00'; }
    var strArray = str.split('*');
    var newStrArray = [];
    for (var i = 0; i < strArray.length; i += 1) {
        if (strArray[i] === keyword) {
            newStrArray = newStrArray.slice(0, -1);
        }
        else {
            newStrArray = __spreadArray(__spreadArray([], newStrArray), [
                strArray[i],
            ]);
        }
    }
    return newStrArray.join('*');
};
exports.goBack = goBack;
var goToHome = function (str, keyword) {
    if (keyword === void 0) { keyword = '0'; }
    var strArray = str.split('*');
    var newStr = str;
    for (var i = strArray.length; i >= 0; i -= 1) {
        if (strArray[i] === keyword) {
            newStr = strArray.slice(i + 1).join('*');
            break;
        }
    }
    return newStr;
};
exports.goToHome = goToHome;
var ussdRouter = function (str, goToHomeKeyword, goBackKeyword) {
    if (goToHomeKeyword === void 0) { goToHomeKeyword = '0'; }
    if (goBackKeyword === void 0) { goBackKeyword = '00'; }
    return exports.goBack(exports.goToHome(str, goToHomeKeyword), goBackKeyword);
};
exports.ussdRouter = ussdRouter;
//# sourceMappingURL=index.js.map